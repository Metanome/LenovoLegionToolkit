<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.ITSModeConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:LenovoLegionToolkit.WPF.Behaviors"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="500"
    Height="550"
    MinWidth="400"
    MinHeight="400"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False"
                Text="{x:Static resources:Resource.ITSModeConfigWindow_Title}" />
        </Grid>

        <custom:InfoBar
            Grid.Row="1"
            Margin="12,0,16,16"
            IsClosable="False"
            IsOpen="True"
            Severity="Informational"
            Title="{x:Static resources:Resource.ITSModeConfigWindow_Description}" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel
                x:Name="_modeCardsPanel"
                Margin="12,4,16,12"
                AllowDrop="True" />
        </ScrollViewer>

        <Grid
            Grid.Row="3"
            Margin="12">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <wpfui:Button
                Grid.Column="0"
                MinWidth="120"
                Appearance="Secondary"
                Click="DefaultButton_Click"
                Content="{x:Static resources:Resource.Default}" />
            <wpfui:Button
                Grid.Column="2"
                MinWidth="120"
                Margin="12,0,0,0"
                Appearance="Primary"
                Click="ApplyButton_Click"
                Content="{x:Static resources:Resource.Apply}" />
            <wpfui:Button
                Grid.Column="3"
                MinWidth="120"
                Margin="12,0,0,0"
                Appearance="Secondary"
                Click="CancelButton_Click"
                Content="{x:Static resources:Resource.Cancel}" />
        </Grid>

    </Grid>
</local:BaseWindow>
